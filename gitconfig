[user]
  name = Alex Lance
  email = code@alexlance.com

[color]
  ui = auto

[core]
  excludesfile = ~/.gitignore
  pager = less -XF

[alias]
  st = status -sb
  co = checkout
  b = branch -v
  l = log --oneline -20 --format=format:'%ad %Cgreen%h %Cred%an %Creset%s' --date=short
  ll = log --oneline --format=format:'%ad %Cgreen%h %Cred%an %Creset%s' --date=short
  lll = log --oneline --format=format:'%ad %Cgreen%h %Cred%an %Creset%s' --date=short --name-status
  pp = !git pull --rebase && git push
  ps = !git stash && git pull --rebase && git stash pop
  pps = !git stash && git pull --rebase && git push && git stash pop
  a = !cd -- ${GIT_PREFIX:-.} && git add -N $@ && git add -p $@ && git commit && git status -sb || git reset
  pr = !git fetch origin pull/$1/head:$1 && git checkout $1 && git diff --name-only $1 $(git merge-base FETCH_HEAD master) && echo

#pr = ! sh -c '(git push origin $(git symbolic-ref --short -q HEAD) && hub pull-request)'
#prs = ! sh -c 'hub pr list -f \"%U [%S] [%au] [%NC comments]%n%t%  l%n%b\"'
#git pr list
#git pr checkout
#git pull-request
# make a pull requestai   pr
# list all pull requests  prs
# checkout a pull request prco

[filter "noeol"]
  clean = noeol_filter
  smudge = cat

[interactive]
  singlekey = true

[push]
	default = current

[pull]
	default = current
  rebase = true

[branch]
    autosetuprebase = always
