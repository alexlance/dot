
escape C-space
startup_message off

#escape Pause
#hsplit
#focusright
#exec urxvt -e screen -RD
#exec firefox
#bind M-1 focusleft
#definekey top M-1 focusleft
#definekey top M-2 focusright

bind left focusleft
bind right focusright
bind up focusup
bind down focusdown
bind x exec xtrlock
definekey top M-1 prev
definekey top M-2 next
definekey top M-3 nextscreen
definekey top M-Tab focusprev
definekey top M-F2 exec
definekey top M-4 exec ratpoison -c "swap 1" -c "nextscreen"
definekey top C-Tab focusprev

#exec rpws init 4 -a
#definekey top C-M-Left rpwsp
#definekey top C-M-Right rpwsn

definekey top C-M-BackSpace quit
definekey top Pause exec bash /home/alla/bin/mute.sh
definekey top F7 exec bash /home/alla/bin/mute.sh
definekey top XF86AudioMedia exec bash /home/alla/bin/mute.sh
definekey top XF86HomePage exec bash /home/alla/bin/heater.sh
definekey top Scroll_Lock exec bash /home/alla/bin/heater.sh

# laptop shortcuts
definekey top F2 exec bash /home/alla/bin/mute.sh
definekey top F4 exec bash /home/alla/bin/volume.sh up
definekey top F3 exec bash /home/alla/bin/volume.sh down

set border 0
set barpadding 0 0 0 0

#exec rpws init 2 -k

# To switch windows 
# First see what numbers your windows are:
# ctrl-space i
# Then switch the two windows:
# ctrl-space :number 1 0

# focus policy
#http://lists.gnu.org/archive/html/ratpoison-devel/2014-09/msg00021.html
# apt-get source ratpoison
# apt-get build-dep ratpoison
# also may need to: apt-get install texinfo  (to obtain makeinfo)
# cd ratpoison
# wget http://lists.gnu.org/archive/html/ratpoison-devel/2014-09/txtstRdKbW0CE.txt
# patch -p1 < txtst...txt
# mv txtthingo /awaysomewhere
# dpkg-source --commit
# dpkg-buildpackage -us -uc (I seemed to need to run this twice?)
# it should have created a ../ratpoison_1.4.8-1_amd64.deb
focus_policy ffm


